# Frontend Environment Variables for Vercel Deployment
# Only VITE_ prefixed variables are exposed to the frontend

# Supabase Configuration (Required for Frontend)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Backend API Configuration (Leave empty when using Supabase as backend)
VITE_API_BASE_URL=