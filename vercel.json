{
  "version": 2,
  "buildCommand": "vite build",
  "outputDirectory": "dist/public",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    "build": "vite build",
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
